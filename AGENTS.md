# AGENTS.md

이 저장소는 김영은 님이 영국 AI Engineer 취업을 목표로 진행 중인 학습 로드맵의 실행 기록입니다. 이 파일은 ChatGPT Codex를 포함한 코딩 에이전트가 이 저장소에서 작업할 때 지켜야 할 규칙입니다.

## 저장소 구조

- `week01/`, `week02/`, `week03/`, … — 주차별 실습 노트북과 README. 매일의 실습 커밋이 쌓이는 곳.
- `papers/` — 읽은 논문의 리뷰 노트(마크다운). `papers/sources/`에 원본 PDF 보관.
- `planning/curriculum/ai_curriculum_v{N}.md` — 마스터 로드맵 커리큘럼. **버전 파일은 절대 삭제·덮어쓰지 말 것** — 개정 시 항상 `v{N+1}` 새 파일을 추가. 최신 버전이 현재 유효한 커리큘럼.
- `planning/weekly_plans/w{N}_execution_plan[_v{M}].md` — 마스터 커리큘럼을 주차 단위로 구체화한 실행 계획. 이 폴더도 버전을 남기며 쌓는 방식(삭제 금지).
- `planning/progress-log.md` — 주 단위로 "로드맵이 실행 가능한지/일관성 있는지/적절한지"를 되돌아보는 회고 기록. **이미 기록된 과거 주차 내용은 소급 수정하지 말 것** (실제 있었던 일의 기록이므로).
- `planning/roadmap-checkin-*.md` — 격주 로드맵 점검 결과 기록.
- `planning/paper-review-template.md` — 논문 리뷰 작성 템플릿.

## 작업 시 지켜야 할 규칙

1. **학습 가이드형 원칙 (매우 중요):** 알고리즘·모델·로직 구현 과제를 도와줄 때, 완성된 정답 코드를 그대로 작성해주지 말 것. 대신 학습 목표, 핵심 개념 설명(코드 예시 없이), 함수 시그니처/입출력 스펙(본문 없음), 그리고 스스로 답해볼 확인 질문 2~3개를 제시하는 방식으로 도와줄 것. 막혔을 때도 정답 코드가 아니라 개념·검색 키워드로 안내할 것. 단, 환경 설정/보일러플레이트(`pip install`, 폴더·venv 생성 등)는 그대로 코드로 제공해도 됨. (이유: 사용자가 완성 코드를 베끼는 것은 학습 효과가 없다고 직접 판단했음.)
2. **버전 파일 삭제 금지:** `planning/curriculum/`, `planning/weekly_plans/` 아래 기존 버전 파일을 절대 삭제하거나 덮어쓰지 말 것. 항상 새 버전 파일을 추가.
3. **과거 기록 소급 수정 금지:** `planning/progress-log.md`에서 이미 "완료"로 기록된 주차 내용은 고치지 말 것.
4. **커밋 습관:** 이 저장소는 매일 실습 커밋이 쌓이는 학습 일지이기도 하다. 의미 있는 변경 단위로 커밋하고, 커밋 메시지는 기존 스타일("W3 Day5: ...")을 따를 것.
5. **언어:** 로드맵/계획 문서는 한국어로 작성된 기존 톤(이모지 헤더, 표 형식 등)을 유지할 것.

## 참고

- 이 저장소는 원래 Claude(claude.ai Project "AI Engineer가 되기 위한 로드맵 수행")와 함께 관리되어 왔고, 로컬에는 격주로 커리큘럼을 자동 점검·개정하는 스케줄 작업이 별도로 존재한다. Codex에서 커리큘럼이나 weekly_plans를 수정한 경우, 다음에 그 자동 점검이 실행될 때 최신 상태를 읽어가도록 반드시 git commit(및 가능하면 push)까지 완료할 것 — 커밋되지 않은 변경은 다른 도구에서 보이지 않는다.
